# ORRERY 기획서

**항성계 창작 도구** — AI 롤플레이용 세계·인물·프롬프트를 만드는 정적 웹 앱

버전 기준: 2026-09 현재 구현 · 짝 문서: `FEATURES.md`(무엇이 있는가), 이 문서(왜 그렇게 만들었는가)

---

## 1. 한 줄 정의

> 캐릭터 카드와 로어북을 **읽고 → 후보를 뽑고 → 펼치고 → 검증하는** 공정으로 만드는 도구.
> 앱 폴더의 `index.html`을 브라우저에서 바로 열어 사용하고, 사용자의 API 키로 직접 호출하며, 결과는 SillyTavern 등에서 바로 쓸 수 있는 형식으로 나온다.

## 2. 왜 만들었나 (문제)

1. **한 방 생성의 품질 한계** — "캐릭터 만들어줘" 한 번으로는 클리셰 덩어리가 나온다. 좋은 결과는 재료 이해 → 방향 선택 → 전개 → 검증의 단계를 거쳐야 나온다.
2. **재료가 크다** — 로어북은 수만 토큰. 매번 통째로 보내면 비싸고 상한에 걸린다. 요약(digest)의 캐싱과 재사용이 필요하다.
3. **설치 장벽** — 대상 사용자는 개발자가 아니다. npm도 서버도 없이, 파일 더블클릭으로 열려야 한다.
4. **기존 도구(ST-Persona-Forge 등)의 흔적** — 참고는 하되 구조·이름·UI 어디에도 티가 나면 안 된다는 요구.

## 3. 대상 사용자

- AI 롤플레이(SillyTavern, 마리나라 등) 사용자. 한국어 중심.
- API 키는 있으나 코드는 모른다. 앱 폴더 하나와 브라우저만 있으면 되어야 한다.
- 결과물을 카드 PNG/JSON, ST 월드인포로 내보내 즉시 쓰고 싶어 한다.

## 4. 핵심 설계 결정과 이유

### 4-1. 빌드 없는 정적 파일 구조
- **결정**: npm·번들러 없이 `index.html`이 CSS와 일반 스크립트를 순서대로 읽는다. 기능 코드는 `js/`, 배경은 `assets/`에 둔다. 본문 글꼴의 외부 스타일시트 참조는 별도로 있다.
- **이유**: 파일을 직접 열 수 있는 낮은 설치 장벽을 유지하면서, 커진 코드를 기능별로 고칠 수 있게 한다.
- **대가**: 공유·백업할 때 HTML 하나가 아니라 폴더 전체를 함께 옮겨야 한다. 대신 CSS의 base64 배경과 5천 줄이 넘던 단일 `app.js`를 분리해 수정 범위가 명확해졌다.

### 4-2. 코어 + 어댑터 프로바이더 구조
- **결정**: 프로바이더는 `chat()`(요청 조립)과 `parse()`(응답 추출) 두 함수만. OpenAI 호환은 공통 어댑터(`OAI_LIKE`) 재사용. 14종 지원.
- **이유**: 새 프로바이더 추가가 함수 두 개 작성으로 끝난다. 마리나라 확장판 이식 시 이 층만 교체하면 된다.

### 4-3. 가변 공정 (정리 → 별씨앗 → 확장 → 검증)
- **결정**: `digest → seed(+cross) → expand(+patch) → check` 파이프라인. 양식별로 단계 생략 가능(skipSeed/skipCheck).
- **이유**:
  - **정리**: 큰 재료를 한 번 압축해 캐싱하고, 확정된 것·추론·빈틈·질문을 화면에서 확인한다. 반환 스키마가 양식마다 달라도 실제 키를 모두 표시한다.
  - **별씨앗**: 방향 5개를 먼저 보고 고르게 한다. 실패한 방향에 풀 토큰을 쓰지 않는다. 둘을 섞는 교배(cross)로 탐색 폭 확대.
  - **확장**: 칸(schema) 단위 생성. 칸이 잠금·재굴림·편집의 단위가 된다.
  - **검증**: 모델이 자기 결과의 위반을 인용과 함께 지적. "이 문제 어떻게 할까?"로 문답에 연결.
- **분류별 1단계 이름**: 설계 준비 / 재료 정리 / 요구 정리. 별씨앗이 없는 양식도 정리 내용을 확인한 뒤 다음 단추로 결과를 만든다.
- **가변 번호**: 숨긴 단계의 고정 번호를 남기지 않고 현재 보이는 단계만 1부터 다시 센다.
- **미정리**: 2단계 표기가 "별씨앗"(스파인·제목·옵션)과 "씨앗 뽑기"(버튼)로 갈려 있다. 한쪽으로 통일할 것.

### 4-4. 한 번에 만들기 (병행, 대체 아님)
- **결정**: 공통 만들기 버튼은 입력 상자 밖 오른쪽에 둔다. 두 방식 카드 안에 효과와 기본 API 호출 횟수를 직접 표시하고 생성 도움말 ?는 제거한다. 일반 한 번에 생성은 기본 1회이며 기존 expand 지시문을 재활용한다. 캐스트는 인원에 따라 다회 호출하며 두 카드 모두 실제 구성에 맞게 안내한다.
- **이유**: 방식 선택에 필요한 차이와 호출 수는 선택하는 자리에서 비교하고, 언어·톤·후보 수·검증 여부 같은 세부 조정만 추가 설정에 접는다.
- **한 번에 만들기의 화면 이동**: 입력 → 상태·실제 호출 차수·중지 버튼을 가진 로딩 화면 → 결과. **입력으로 돌아가기**는 입력과 기존 결과를 보존하고 **결과 다시 보기**는 API 없이 연다. 실패·중지 시 이전 작업을 유지한다.
- **단계별 이동**: 입력 → 정리 확인 → 후보 선택 → 결과를 한 화면씩 표시한다. 다음 버튼으로 진행하고 후보를 고르거나 둘을 섞는 동안에는 후보 화면에 머문다. 후보 없는 양식도 정리 확인 후 다음 버튼으로 결과를 만든다. 단계선은 이미 만든 단계만 다시 열며 검증은 결과의 추가 도구에 둔다. 검증 중 중지해도 완성 결과는 보존한다. 캐스트는 별도 목록 화면과 개별 결과 왕복을 제공하고 중지 시 완성한 인물을 남긴다.
- **한 번에 만들기의 입력 조건**: 선택한 재료 또는 구상 중 하나만 있으면 실행한다. 재료가 있으면 구상은 필수가 아니라 방향을 덧붙이는 선택 입력이다.
- **중단 위치**: 작업대는 로딩 화면에 중단 단추를 표시하고, 다른 화면에서는 현재 실행 중인 단추 옆에 붙인다. 어떤 요청을 멈추는지 시선 이동 없이 알 수 있어야 한다.

### 4-5. 3분류 = 작업대의 3모드
- **결정**: 세계(항성)·인물(행성)·프롬프트(궤도)는 별도 화면이 아니라 **작업대 하나의 모드**. 내비에서 분류를 누르면 그 모드의 작업대로 직행.
- **기존 자료 다듬기**: `보충`을 단순 추가 작업으로 한정하지 않는다. 기존 자료를 다루는 방식에서는 균형 있게 / 단순하게 / 자세하게 / 다시 설계 중 하나를 고르며, 기본값도 추가와 확장이 아니라 삭제·통합·보완을 함께 판단하는 `균형 있게`다.
- **이유**: 분류에 따라 실제로 바뀌는 건 작업대뿐. 재료·기록·대화는 공유다.
- **분류별 분리 저장**: 요약(`digestBy`)과 통합 구상·요청(`briefBy`)을 분류별로 저장한다. 기존 `extraBy`는 각 구상 뒤에 추가 조건으로 병합하고 최종 출력의 `convert.extraBy`는 별도 유지한다. `workBy`에는 양식·후보·선택·결과·잠금·문답·검증·캐스트·관계·최종본·이어쓰기/다시 쓰기 지시와 `project.screen`을 보관한다. 이전 `card.inputOpen`도 해석한다. 정리 지시문이 다른 양식이면 경고한다.
- **전환 시 복원**: 떠나는 분류의 진행 상태를 보관하고 돌아올 때 복원한다. 저장된 양식이 현재 분류에서 사용 가능할 때 해당 작업을 복원하며, 다른 양식으로 직접 바꾸는 것은 결과 초기화와 구별한다. 재료와 기록은 공유한다. 대화 역할은 분류를 따라가되 평가자·역할 없음은 사용자의 선택으로 보고 유지한다.

### 4-6. 재료는 공유 자원
- **결정**: 드롭존 하나에 6종 자동 판별. 재료함은 분류를 넘나들며 공유하고 이름·본문·필드를 수정한다. 작업대에는 선택 개수와 **재료 추가·관리**를 바로 표시하고 목록은 접는다. 성운 밖의 관리 버튼은 현재 탭을 유지한 모달을 연다. 기존 재료 화면 DOM을 옮겨 사용하고 닫을 때 복원해 입력과 이벤트를 보존한다.
- **이유**: 세계로 만든 로어북을 곧장 인물 재료로 → **연쇄 제작**(세계→인물→프롬프트)이 이 도구의 핵심 루프. "재료로 넣기" 버튼이 그 연결선.
- 카드 내장 로어북 분리, 지시문 의심 항목 자동 흐리기(재료는 설정이지 명령이 아니다).
- **용도 배지**: 세계·인물·프롬프트는 같은 회청색으로 표시한다. 같은 수준의 분류가 색 때문에 중요도 차이로 읽히지 않게 하고, 금색은 활성·선택 상태에 남긴다.

### 4-7. 프리셋 = 양식 (ST 구조 차용)
- **결정**: 양식마다 schema(출력 칸)·stages(공정별 지시문)·common(공통 지시문)을 가진다. 전부 접이식 목록으로 편집. 가져오기는 Orrery/ST 형식 자동 판별, ST의 사용자 구획은 공통 지시문으로 흡수.
- **이유**: ST 사용자에게 익숙한 멘탈 모델(토글 가능한 프롬프트 구획 목록). 공통 지시문은 "모든 공정 앞에 들어갈 시스템 프롬프트" 요구의 답.
- **내장 14종**: 각 양식은 검증된 스킬 문서(창작 엔진, 그리팅 규칙, 성향 프로토콜 등)를 이식한 것. 프롬프트가 곧 제품.
- **프로필 양식 설계(JSON)**: 설정집을 읽어 그 세계 전용 칸 구성을 뽑고, 버튼 하나("이 양식 등록하기")로 즉시 새 양식 등록 — 도구가 도구를 만든다.
- **호칭 통일(2026-08)**: 같은 대상을 헤더는 "양식", 화면 제목은 "프롬프트", 셀렉트 라벨은 "프리셋"이라 부르던 것을 전부 "양식"으로 맞췄다. "프리셋"은 변수·id와 ST 형식 이름에만 남긴다.
- **점검 양식은 하나가 아니라 셋**: 범용 `평가 · 보강 제안` 하나로 시작했으나 분류가 생기면서 세계/인물/프롬프트 점검으로 쪼갰고, 원본은 뼈대(`auditBase()`)로만 남기고 목록에서 은퇴시켰다. 분류를 이미 고른 사용자에게 "이 분류의 점검"과 "범용 점검"을 동시에 내미는 것은 고를 근거 없는 선택지다. 다만 원본에만 있던 `살아 있는 곳`(무엇이 작동하는가) 칸은 셋 다에 이식했다 — 점검이 문제만 나열하면 어디를 건드리면 안 되는지 알 수 없다.

### 4-8. 결과에 질문하기 (문답 루프)
- **결정**: 설계 상담역 / 당사자 본인 2모드. 답에 딸린 제안 문안은 원클릭 반영. 검증 지적에서 바로 진입.
- **이유**: 생성→검토→수정 루프를 채팅으로 풀되, 반영은 구조화(칸 단위)로. "대화는 자유롭게, 결과는 칸으로."
- **교신 컨텍스트**: 작업 재료 / 정리한 내용 / 지금 만든 것을 하나의 선택기에 모으되 각각 독립적으로 포함한다. 재료만 별도 위치에서 고르게 해 현재 결과를 함께 보낼 수 있다는 사실을 숨기지 않는다.
- **교신 화면**: 큰 화면에서는 왼쪽에 역할·보낼 내용, 오른쪽에 대화·입력창을 배치한다. 페이지 전체 대신 각 목록을 스크롤하고 입력창은 화면 안에 둔다. 980px 이하에서는 설정을 접어 대화를 우선한다. 설정과 대화는 서로 다른 작업 페이지가 아니므로 탭 전환보다 접기를 사용한다.
- **교신 입력과 마무리**: 토큰 정보 오른쪽에 작업대 반영·대화 요약을 모으고 좁은 화면에서는 다음 줄 오른쪽으로 내린다. 입력칸과 보내기는 기본 높이 48px, 입력칸만 내용에 따라 최대 160px(700px 이하에서는 120px)까지 자동 확장한다. 보내기는 같은 테두리 안 오른쪽 아래에 고정해 긴 입력에서도 위치가 흔들리지 않게 한다.
- **히스토리 제외**: 눈 버튼은 문답 단위로 전송 여부만 바꾸고 원문은 보존한다. 질문만 빠진 답변의 맥락 오류를 피한다. 요약도 별도로 제외 가능하며 채팅·요약·작업대 반영·재료화 모두 같은 필터를 쓴다. 전체 복사는 보관용으로 제외 내용을 포함한다.
- **대화 실패 복구**: 전송 실패·중지 시 사용자 메시지를 남기고 다시 보내기/삭제를 제공한다. 실패한 메시지를 처리하기 전 새 전송을 막으며, 작성 중인 입력도 따로 저장한다. 응답 중 전송을 반복해도 같은 대화를 중복 호출하지 않는다.
- **결과 가독성**: 긴 세계·인물·프롬프트의 모든 칸을 기본 펼침으로 나열하지 않는다. 제목과 미리보기를 가진 접이식 카드로 두고 첫 카드만 열며, 여러 팝업 대신 같은 문서 흐름에서 비교·수정한다.

### 4-9. 기록 자동화
- **결정**: 생성 즉시 자동 저장, 같은 작업 이어 고치면 갱신(libId). ★고정만 수동, 나머지는 200개 초과 시 정리.
- **이유**: "저장 안 했는데 날아감"을 없앤다. 사용자가 관리할 것은 고정 여부뿐.

### 4-10. 마지막 작업 복구
- **결정**: 현재 공정·옵션과 `workBy`의 다른 분류 작업을 임시 키(`orrery.draft.v1`)에 보관하고 다음 실행 때 복구 여부를 묻는다. `project.screen`으로 입력·정리·후보·결과·캐스트 화면을 복구한다. 고정하기/캐스트 기록 후에도 임시본을 유지한다. 재료는 기본 저장 키(`orrery.v1`)에 항상 보관한다.
- **이유**: 자동 기록은 완성 결과 중심이라 후보 선택·잠금·문답 같은 제작 진행 상태를 모두 대신하지 못한다. 고정을 완료로 간주해 임시본을 지우지 않고, 작업 전환·재실행 뒤 이어 만들 수 있게 한다.

### 4-11. 안전장치
- 컨텍스트 상한 **전송 전** 차단 + "읽기를 먼저 하라" 안내 (돈 나가기 전에 막는다)
- JSON 해석 실패 시 1회 자동 재시도, 그래도 실패면 로그에 원문 보존
- Esc 중단 · 구버전 저장 키 자동 이관 · 설정 파일 백업/복원
- **저장 실패 표시**: 저장 성공은 잠깐 상태로 알리고, 저장 실패는 화면에 지속 경고와 전체 백업 안내를 표시한다. 읽지 못한 기존 원문은 복구 키에 보존한다.
- **요청 중 작업 보호**: 요청을 실행하는 동안 분류·양식·작업 입력 교체와 다른 요청 실행을 제한한다. 응답 적용 전에도 작업이 유효한지 확인해 이전 요청의 결과가 다른 작업에 반영되지 않게 한다.
- **연결 확인 상한**: 테스트 요청은 최대 24토큰을 지정하며 연결별 응답 상한으로 덮어쓰지 않는다.
- **연결만 백업·복원**: 기기를 옮길 때 전체 작업 데이터를 덮어쓰지 않도록 API 연결 전용 JSON 백업을 제공한다. 키·업체·모델·주소·옵션을 함께 옮기며, 같은 id는 갱신하고 다른 연결과 작업은 보존한다. 키를 생략한 파일은 기존 키를 지우지 않는다. 평문 키 포함 사실은 내보내는 위치에서 안내한다.
- **키보드 조작**: 방식과 후보는 버튼으로 선택한다. 모달은 열 때 내부로 포커스를 옮기고, Tab 이동을 내부에 유지하며 닫으면 원래 위치로 돌려준다.

### 4-12. 첫 작업의 입력 흐름
- **입력 화면 배치**: 양식·작업 방향 → 구상·요청 → 재료 선택/추가·관리 → 한 번에/단계별 선택 → 접힌 추가 설정 → 오른쪽 만들기. 두 생성 방식 모두 한 번에 한 화면만 표시한다.
- **접기 범위**: 핵심 선택과 실행은 바로 보이고 재료 목록·세부 설정·결과 추가 도구는 기본 접기. 결과 내용과 기본 내보내기, 잘린 출력 복구는 바로 접근한다. 쉬운 모드에서도 두 방식과 추가 설정을 사용할 수 있으며 선택을 유지한다.
- **문구와 선택 수**: 구상 라벨의 ‘요청·선택’, 반복 안내문, 첫 화면의 긴 양식 설명을 제거한다. 프롬프트 다듬기와 변형은 실제 지시가 다르므로 내부 기능을 유지하되 시작 선택은 ‘기존 프롬프트 수정’으로 합치고 목적은 추가 설정에서 고른다. 카드 제목은 위쪽 정렬하고 방식 설명은 짧은 명사형으로 쓴다.
- **입력 화면 높이**: 구상은 기본 2줄·80px로 시작하되 수동 세로 확대를 허용한다. 재료 제목·선택 개수·관리 버튼은 한 줄에 모으고 제목만 굵게 표시한다. 기본 화면의 불필요한 세로 스크롤을 줄이되 설정을 펼치거나 입력칸을 늘리면 자연스럽게 스크롤한다. 좁은 화면에서도 만들기 방식은 두 카드를 나란히 유지하고 설명을 줄바꿈한다.
- **첫 연결**: 연결이 준비되지 않았으면 만들기 버튼을 **연결 설정하고 만들기**로 바꾸고 설정 화면을 연다. 입력한 구상은 유지하며, 설정 후 작업대로 돌아와 만들기를 눌러야 생성한다. 연결 화면은 업체·키·모델을 먼저, 주소·응답 길이 등은 세부 설정에 둔다.
- **쉬운 모드의 수정·복구**: 결과 직접 편집과 **이 칸만 다시**를 남기고, 잘린 응답에는 **이어서 만들기**를 표시한다. 복잡한 옵션을 감추더라도 오류를 복구하는 동작은 유지한다.

## 5. 정보 구조

```
헤더(78px, ≤720px에서 66px): [궤도 로고 ORRERY] ··· [● 연결 알약▾ 확인] │ 연결 로그 양식 도움말(?)
                                                           ≤720px에서는 오른쪽 넷이 ☰ 메뉴로 접힘
내비(72px, ≤640px에서 60px): 성운(재료) │ 항성(세계) 행성(인물) 궤도(프롬프트) │ 교신(대화) 성도(기록)
                                       └── 이 셋은 작업대의 모드 ──┘
```

- 왼→오 = 천체 형성 순서 = 작업 흐름: 성운(재료)이 뭉쳐 항성(세계)이 되고, 행성(인물)이 생기고, 궤도(프롬프트)를 돈다. 교신은 관제 통신, 성도는 관측 기록.
- 헤더는 관리(연결·로그·양식·도움말), 내비는 작업. 행동 버튼(한 번에 만들기 등)은 메뉴가 아니라 맥락(작업대) 안에.
- **내비 라벨**: 넓은 화면은 기능어(11px, 위) + 은유(16px, 아래). 640px 이하에서는 재료·세계·인물·프롬프트·대화·기록의 기능어(14px)와 아이콘을 남기고 은유를 감춘다. 좁은 화면에서도 목적지를 바로 읽을 수 있게 한다.

## 6. 디자인 언어

- **컨셉**: 우주 관측 도구. 유리 돔 안의 놋쇠 장치.
- **리퀴드 글래스**: 사진 배경(cosmic-drift) + 비네트 + 노이즈 / 패널은 그라디언트+블러+상단 글로우 라인. 굵은 베벨은 쓰지 않는다. 남아 있는 `inset 0 1px 0`은 알파 0.03~0.1의 상단 글로우 라인 한 줄로, 테두리가 아니라 빛으로 읽히는 범위 안에서만 허용.
- **금색(#e9b654)은 상태 언어**: 활성·선택·진행만. 장식으로 쓰지 않는다.
- **타이포**: 고딕 통일(명조 폐지). 본문 Pretendard, 계기판(로고·숫자·로그·메타)은 --mono. 크기 3단계: 12(라벨·메타) / 14(본문·입력) / 16+(제목·내비), 터치 기기 입력만 16.
- **아이콘**: 전부 currentColor SVG. 이모지 금지(렌더링 비일관) — 칸 잠금의 🔒/🔓도 SVG 자물쇠로 교체됨.
- **도구 2계층**: 패널 머리 사각 버튼(화면 전체 행동) vs 행 도구 투명 버튼(항목 하나 행동).
- **모션**: 첫 로드 스태거 1회만, 전환은 transition(중단 가능), scale(.96) 프레스, reduced-motion 전부 대응.

## 7. 검증 체계

- [jsdom 동작 회귀 검사](tests/README.md): 실제 HTML과 앱 스크립트를 로드해 입력 노출, 분류별 작업 복원, 임시 작업, 생성·중지, 대화 실패·재전송·중복 요청, 연결 확인 상한, 모달 포커스, 쉬운 모드 복구 버튼을 확인한다. API는 가짜 응답으로 대체하며 실제 브라우저 렌더링·업체 연결·생성 품질 검사는 포함하지 않는다.
- **Chrome/Edge 배치 검사**: 별도 임시 프로필에서 실제 렌더링을 측정한다. 1920×1080/960/920의 세 작업대 기본 화면, 320~768px 작업대 폭, 다섯 화면 크기의 교신 배치·입력 자동 확장·설정 접기·재료 행을 검사하고 스크린샷을 남긴다. 실제 키와 업체 API는 사용하지 않는다.
- CSS 괄호 균형 검사 + 치환마다 assert + 단계별 백업(대량 수정 사고 재발 방지).
- **문서 대조도 검증 항목에 포함**: 화면 제목·버튼 문구·양식 개수는 코드가 정답이다. 문서와 어긋나면 문서를 고치거나 코드를 고치되, 어긋난 채로 두지 않는다.
- **확인하지 않은 것**: 실물 기기의 터치·가상 키보드 조작, 실제 업체 API 연결·14양식의 생성 품질, 프로바이더별 CORS, Safari 저장, ST 호환. 모의 응답과 Chrome/Edge 배치 검사는 이 검증을 대신하지 않는다.

## 8. 로드맵 (미착수/보류)

| 항목 | 상태 | 메모 |
|---|---|---|
| 내비 라벨 위계(기능어 vs 은유) | **완료** | 넓은 화면은 이중 표기, 좁은 화면은 기능어+아이콘 |
| 화면 이름 충돌 정리 | **완료** | v-log → "로그", v-prompts → "양식", 셀렉트 라벨도 "양식" |
| "별씨앗 / 씨앗 뽑기" 표기 통일 | 대기 | 버튼만 옛 이름으로 남음 |
| `평가 · 보강 제안` 양식 | **완료(은퇴 확정)** | 양식은 되살리지 않고 `살아 있는 곳` 칸만 점검 3종에 이식 |
| "기본값으로 되돌리기"의 범위 | **완료** | `builtinPresets()`에서 같은 id를 찾아 그 양식의 기본값으로. 직접 만든 양식은 거부 |
| 내장 양식 갱신 이관 | **부분 완료** | `working` 칸은 init에서 표적 이관. 범용 rev 태그+병합은 다음 내장 양식 수정 때 도입 |
| 확장 잠금 도구 위계 | **완료** | 전부 잠금/해제 → 패널 머리 iconbtn, 칸 잠금 이모지 → SVG |
| 대화·기록 도구 위계 | **완료** | 화면 전체 행동(비우기·재료로·복사·내보내기)을 패널 머리로 |
| 작업대 제목 | **완료** | studioTitle "○○ 작업대" — 분류 전환 연동 |
| 만들기 방식 안내 | **완료** | 각 선택 카드에 효과·기본 호출 횟수 표시, 생성 ? 제거 |
| 초저폭 대응 | **완료** | 지원 하한 280px(구 320 — 폴드 커버 대응, overflow-x:hidden이 우측 잘림을 숨기던 원인). ≤430px 워드마크 생략, 스파인 row-gap, 그리드 minmax→min() 클램프, ≤640 flex 자식 min-width:0 가드 |
| 저장 차단 안내 승격 | **완료** | 성공은 짧은 상태, 실패는 지속 경고+토스트. 손상 원문은 recovery 키 보존 |
| 14양식 실LLM 품질 라운드 | 대기 | 점검 결과 기반으로 지시문 수정 |
| 프로바이더 CORS 실측표 | 대기 | 사용자 브라우저 테스트 필요 |
| 내장 양식 별도 데이터 파일 분리 | 검토 | 큰 `studio.js`를 더 나눌 만큼 수정 빈도가 높아질 때 진행 |
| 마리나라 확장판 | 보류 | 어댑터 층 교체로 이식 (marinara.apiFetch) |
| 시안 룩의 나머지 화면 확장 | 진행 중 | 재료 화면은 1:1 완료, 작업대·양식 등은 표면 문법만 상속 |

## 9. 변경 시 규칙

1. **`FEATURES.md`의 기능은 UI 개편에서도 유지된다.** 빼려면 명시적 합의.
2. 새 세션 시작 시 `index.html` + `FEATURES.md` + 이 문서(`DESIGN.md`)를 함께 제공한다.
3. 대량 문자열 치환은 assert 필수, 작업 전 백업, 후에 CSS 균형·node --check·스모크.
4. 프롬프트(양식 지시문) 수정은 코드 수정과 같은 무게로 취급 — 그것이 제품이다.
5. UI 문구를 바꾸면 같은 커밋에서 세 md의 해당 표현도 바꾼다.
