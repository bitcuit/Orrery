# ORRERY 기능 목록 (헌장)

UI를 어떻게 바꾸든 **이 목록의 기능은 사라지면 안 됩니다.**
코드에서 실측한 것: 화면 7개 · 버튼 93개(정적 마크업 63 + 스크립트 템플릿 30) · 내장 양식 14종 · 프로바이더 14종.
새 대화 세션을 시작할 때 이 파일을 함께 주면 기능 누락을 막을 수 있습니다.

---

## 화면 (7)

| 내비 이름 | 실제 기능 | id | 화면 제목 |
|---|---|---|---|
| 성운 | **재료 넣기** | v-sources | 재료 넣기 |
| 항성 / 행성 / 궤도 | **작업대** (세계 / 인물 / 프롬프트 모드) | v-studio | (단계별 패널) |
| 교신 | **대화** | v-talk | 대화 |
| 성도 | **기록** | v-library | 기록 |
| (헤더) 양식 | 프리셋 편집 | v-prompts | 양식 |
| (헤더) 연결 | API 연결 관리 | v-settings | 연결 |
| (헤더) 로그 | 실행 로그 | v-log | 로그 |

화면 제목은 그 화면을 여는 버튼 이름과 같게 유지한다. 사용자 대면 명칭은 **양식**으로 통일하고 "프리셋"은 내부 용어(변수·id·ST 형식 이름)로만 쓴다.

내비 동작: 항성/행성/궤도 클릭 → 그 분류의 작업대로 직행. 같은 분류 재클릭 → 작업대 복귀.
작업대 밖에서는 현재 분류에 흐린 점(pending) 표시.
내비 라벨은 이중 표기 — 기능어(11px, 위) + 은유(16px, 아래). 640px 이하에서 기능어는 숨김.
헤더는 720px 이하에서 햄버거(btnMore → topMore)로 접힘: 연결 · 로그 · 양식 · 도움말. 430px 이하에서는 워드마크(ORRERY 글자)를 숨기고 궤도 마크만 남겨 햄버거 자리를 확보.
지원 최소 폭 280px(html min-width) — 씨앗·모드 그리드는 `minmax(min(Npx,100%),1fr)`로 좁은 폭에서 자체 수축.
작업대에는 분류 이름이 붙은 제목이 있다(studioTitle: "세계 작업대" 등) — 다른 화면의 [재료 넣기][대화]와 같은 층위.

---

## 1. 재료 (성운)

- 드롭존 하나로 자동 판별: **PNG 카드(tEXt) · CHARX · 카드 JSON(V1~V3) · 로어북 · ST 월드인포 · TXT/MD**
- 여러 파일 한꺼번에 가능. 카드에 내장된 로어북은 **별도 항목으로 분리**
- **직접 붙여넣기**: 내부 스크롤 입력창, 글자/토큰 수 실시간, 첫 줄→이름, Ctrl+Enter
- 카드마다: **포함 체크박스** · 종류 아이콘 · 토큰 추정 · 빼기(✕)
- 카드마다 **수정**: 재료 이름과 텍스트 본문, 캐릭터 각 필드, 로어북 항목 이름·키워드·본문을 직접 편집. 이번 수정 취소 · 최초 원본과 비교 · 원본 복원
- **복제**: 현재 수정 상태를 독립된 새 재료와 새 원본으로 복제
- 로어북: **항목 고르기** (검색 / 전체 선택 / 해제 / **설정 항목만** — 지시문 의심 항목 자동 흐리기 + "지시문?" 표식)
- 캐릭터: **내용** (필드 미리보기)
- **전부 비우기** · 재료 개수 뱃지
- 재료는 세 분류가 공유. 분류별 안내문(matHint) 연동
- 양식별 재료 필요 표시: 필수인데 없으면 경고 + "재료 넣으러 가기"
- 작업대 1단계의 **성운에서 가져오기**: 재료 탭으로 돌아가지 않고 이번 작업에 포함할 재료를 선택

## 2. 작업대 (분류 공통 구조)

- **요청 칸**: 모든 단계에 전달, 분류별로 따로 저장/복원(extraBy)
- **한 번에 만들기**: API 1회, 씨앗·검증 생략, 원문 통째 전송. 옆에 호출 횟수 안내(단계별이면 N회)
- 단계별 공정:
  1. **읽기** — 분류별 이름·문구 (세계 읽기 / 재료 읽기 / 요구 정리). 요약은 **분류별 분리 보관**(digestBy), 다른 양식으로 읽은 요약이면 경고. 지우기
  2. **별씨앗** — 후보 N개, 하나/둘 선택, **고른 둘 섞기**(교배). 건너뛰는 양식 있음(skipSeed)
     ※ 스파인·패널 제목·옵션은 "별씨앗", 실행 버튼만 "씨앗 뽑기" — 표기 통일 대상
  3. **확장** — 칸별 결과. **칸 잠금 / 전부 잠금 / 전부 해제 / 안 잠근 칸만 다시**(추가 지시 입력) / 칸 직접 편집. 전부 잠금/해제는 패널 머리의 iconbtn, 칸별 잠금은 SVG 자물쇠(이모지 아님)
  4. **검증** — 위반 인용, 판정. "이 문제 어떻게 할까?" → 물어보기 연계. 검증 끄기 옵션(skipCheck 양식은 패널 자체를 숨김)
- **구상 칸**: 라벨은 세 분류 모두 "구상", **안내문과 플레이스홀더만 분류별로 다름**
- **작업 방향**: 세계(새로 만들기/기존 세계 보충), 인물(세계에서 뽑기/기존 인물 보충/상대역/관계망), 프롬프트(새로 만들기/기존 프롬프트 보충/변형). 분류별 선택을 따로 기억
- 재료 본문이나 포함 선택이 읽은 뒤 바뀌면 요약에 **다시 읽기 권장** 경고
- 옵션(세부 설정): 출력 언어 · 전체 톤 · 별씨앗 개수 · 캐스트 인원 · 성인 요소 · 검증 단계
- **캐스트**(btnCast "인물 뽑기"): 여러 인물 일괄 + 관계 짜기(btnRelate) + 기록에 넣기(btnCastSave)
- **물어보기**(btnAsk): 설계 상담역 / 당사자 본인 2모드, 제안 문안 원클릭 반영, 문답 비우기
- **내보내기**: 글로 복사 / 글로 내려받기 / 카드 JSON 복사·내려받기 / **PNG 카드** / **로어북으로 뽑기**(btnDlBook, 세계→ST 월드인포+재료 자동 추가) / **재료로 넣기**(분류 연쇄 제작) / **고정하기**(btnSaveLib — ★ 붙여 기록에 저장)
- **이 양식 등록하기**(btnMakePreset): 프로필 양식 설계(JSON) 결과를 즉시 양식으로 등록

## 3. 대화 (교신)

- 상대 역할 5종: 세계관/인물/프롬프트 상담역 · 냉정한 평가자 · 역할 없음
- **역할 프롬프트 편집**: 직접 입력(customTalkPrompts 저장) + 기본 프롬프트 보기 + 기본값으로 되돌리기
- 컨텍스트 체크(같이 보낼 것): 재료 / 세계 요약 / 지금 만든 것 — 분량 표시
- 보내기(Ctrl+Enter) · 패널 머리 iconbtn 3종: 대화 비우기 · **이 대화를 재료로** · 전부 복사
- 분류 바꾸면 상대 역할 연동 — 단 **냉정한 평가자(critic)와 역할 없음(free)은 유지**
- 대화 저장(새로고침 유지)

## 4. 기록 (성도)

- 만들면 **자동 저장**(1.2초 디바운스), 이어 고치면 새 항목 없이 **갱신**(libId)
- ★ 고정 · 이름 변경 · 검색 · 분류 필터(분류 전환 시 자동 연동) · **불러오기**(양식 자동 전환)
- 패널 머리 iconbtn: 전체 내보내기 · 비우기(고정 안 한 것만; 200개 초과 시 자동 정리, 로그에 기록)
- 기록에서 바로 글/JSON/PNG 내보내기

## 5. 양식 (프리셋) — 14종 내장

- 점검 3종(세계·인물·프롬프트) 공통 칸: 한 줄 진단 · **살아 있는 곳** · 흔한 곳 · 채우면 좋을 것 · 정해야 할 질문
- 프롬프트: 프롬프트 생성기 · 롤플레이 프롬프트 · 프롬프트 점검
- 세계: 세계관 설계 · 압축 소개본 · 상세 안내서 · 세계 점검
- 인물: 기본 카드 · 창작 엔진 (풀 시트) · 설정집 프로필 (압축형) · 첫 만남 (그리팅) · 성향 프로토콜 (성인 · 18+) · 인물 점검
- 공통(group:'all'): 프로필 양식 설계 (JSON)

> **비활성 정의**: `presetAudit()` / `평가 · 보강 제안`(id `audit`, group `all`)은 코드에 정의되어 있으나 `builtinPresets()`에 없어 목록에 뜨지 않는다. 세 종류 점검 양식의 뼈대(`auditBase()`)로만 쓰이므로 **지우지 말 것**.
> 은퇴는 확정이다 — `group:'all'`이라 되살리면 세 분류마다 점검 양식이 2개가 되어 고를 근거가 없어진다. 대신 이 양식에만 있던 고유 칸 `working (살아 있는 곳)`은 2026-08에 점검 3종 전부로 이식했다.

- **공통 지시문**: 구획 목록(켜기끄기 토글·이름·역할칩·토큰·위/아래/삭제·접이식 편집). 모든 공정 + 한 번에 만들기 맨 앞에 주입
- **출력 칸**: 접이식(키·이름·지시), 순서 바꾸기, 추가/삭제 — 칸이 곧 잠금·재굴림 단위
- **공정별 지시문**: 접이식(블록 수·토큰·temperature 요약), temperature/max tokens
- 치환 가능한 값: `{{source}} {{digest}} {{seed}} {{card}} {{schemaSpec}} {{lang}} {{tone}} {{seedCount}} {{extra}} {{cast}} {{locked}} {{unlocked}} {{instruction}}`
- 프리셋 도구(아이콘 6개): 복제 · 새로 만들기 · 이름 바꾸기 · 내보내기 · **가져오기(ST 프리셋 포함 — Orrery·ST 자동 판별)** · 삭제 / 그리고 공정별 지시문 아래 기본값으로 되돌리기(btnResetStages — **그 양식의 내장 기본값**으로 되돌림. 직접 만든 양식이면 거부 토스트)
- ST 프리셋 가져오기: 내장 식별자·마커(ST_BUILTIN) 제외하고 **공통 지시문으로** 들어옴

## 6. 연결 — 프로바이더 14종

openai · anthropic · gemini · vertex · openrouter · nanogpt · mistral · cohere · xai · together · venice · pollinations · local(Ollama · LM Studio · koboldcpp) · custom

- 연결 추가/편집/삭제 · 헤더 알약에서 전환 · **확인**(헤더의 btnTest, connDot 초록/빨강) · 모델 목록 받기
- 연결별 파라미터: 응답 토큰 상한 · **컨텍스트 상한(전송 전 차단 + 안내)** · temperature · top_p(Cohere는 p)
- vertex는 project/location/액세스 토큰, pollinations는 키 없음
- **설정 내보내기/가져오기**(btnDataExport/Import) — 연결·기록 등 전체 백업
- 도움말(?): CORS 안내 모달 + `python -m http.server 8777` 복사 (Esc로 닫힘)

## 7. 로그

- 실행 로그 · 전부 복사 · 비우기 · "보낸 지시문 전문까지" 옵션

## 8. 저장·이관

- localStorage `orrery.v1` · 구버전 키(`vivarium.v1` / `terrarium.v1` / `casting.v1`) 자동 이관
- localStorage `orrery.draft.v1`: 저장하지 않은 마지막 작업의 재료·공정·옵션을 임시 보관. 다음 실행 때 복구 여부 확인. 고정/캐스트 기록 시 제거
- 저장 차단(Safari file:// 등) 시 **로그에만** 한 줄 남김 — 사용자가 로그를 열지 않으면 못 본다. 토스트 승격 검토 대상

---

## 동반 파일

- `README.md` — 사용 설명서

## 알려진 검증 한계 (코드로 못 잡는 것)

14개 양식의 실제 프롬프트 품질(LLM 미검증) · 프로바이더별 CORS 실측 · PNG 캔버스 · 실브라우저 렌더링 · Safari 저장 · SillyTavern 호환
