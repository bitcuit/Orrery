# ORRERY 기능 목록 (헌장)

UI를 어떻게 바꾸든 **이 목록의 기능은 사라지면 안 됩니다.**
코드에서 실측한 것: 화면 7개 · 내장 양식 15종 · 프로바이더 14종. 버튼은 정적 마크업과 동적 템플릿이 섞여 있으므로 아래 기능 목록을 기준으로 누락을 검증합니다.
새 대화 세션을 시작할 때 이 파일을 함께 주면 기능 누락을 막을 수 있습니다.

---

## 화면 (7)

| 내비 이름 | 실제 기능 | id | 화면 제목 |
|---|---|---|---|
| 성운 | **재료 넣기** | v-sources | 재료 넣기 |
| 항성 / 행성 / 궤도 | **작업대** (세계 / 인물 / 프롬프트 모드) | v-studio | (단계별 패널) |
| 교신 | **대화** | v-talk | 대화 |
| 성도 | **기록** | v-library | 기록 |
| (헤더) 양식 | 프리셋 편집 | v-prompts | 양식 |
| (헤더) 연결 | API 연결 관리 | v-settings | 연결 |
| (헤더) 로그 | 실행 로그 | v-log | 로그 |

화면 제목은 그 화면을 여는 버튼 이름과 같게 유지한다. 사용자 대면 명칭은 **양식**으로 통일하고 "프리셋"은 내부 용어(변수·id·ST 형식 이름)로만 쓴다.

내비 동작: 항성/행성/궤도 클릭 → 그 분류의 작업대로 직행. 같은 분류 재클릭 → 작업대 복귀.
작업대 밖에서는 현재 분류에 흐린 점(pending) 표시.
내비 라벨은 이중 표기 — 기능어(11px, 위) + 은유(16px, 아래). 640px 이하에서는 기능어(14px)와 아이콘을 남기고 은유를 숨김.
헤더는 720px 이하에서 햄버거(btnMore → topMore)로 접힘: 연결 · 로그 · 양식 · 도움말 · **밝게/어둡게**(btnTheme, 한 번 눌러 라이트/다크 전환. `localStorage orrery.theme`, head 인라인 스크립트로 첫 그리기 전에 적용. styles.css의 리터럴 색은 `--c0…` 변수로 치환되어 `html[data-theme="light"]`에서 명도 반전값을 씀). 430px 이하에서는 워드마크(ORRERY 글자)를 숨기고 궤도 마크만 남겨 햄버거 자리를 확보.
지원 최소 폭 280px(html min-width) — 씨앗·모드 그리드는 `minmax(min(Npx,100%),1fr)`로 좁은 폭에서 자체 수축.
작업대에는 분류 이름이 붙은 제목이 있다(studioTitle: "세계 작업대" 등) — 다른 화면의 [재료 넣기][대화]와 같은 층위.

---

## 0. 첫 진입 · 쉬운 모드

- **첫 진입 온보딩**: 처음 실행(저장 데이터 없음)이면 재료 넣기 화면으로 바로 떨어뜨리지 않고 **환영 모달**(welcomeModal)을 띄운다. "무엇을 만들까요?"와 큰 카드 3장(세계/인물/프롬프트) — 고르면 그 분류의 작업대로 진입, 한 번에 만들기 + 구상 칸 포커스. "재료는 없어도 된다" 안내. 연결이 없으면 "연결 먼저 등록" 안내 줄 표시. 모달 안 체크박스로 쉬운 모드 켜고 시작(신규는 기본 켜짐). 기존 사용자에겐 뜨지 않음
- **쉬운 모드**(S.opts.easy · 헤더 `btnEasy` 토글, 켜지면 강조): 잘리지 않은 결과의 이어쓰기를 감춘다. 기본 OFF, 신규 첫 진입에만 유도. 두 만들기 방식의 선택은 유지
  - **기본 접기**: 재료 목록 · 추가 설정 · 결과의 추가 도구(일괄 다시 쓰기·잠금·생성 원문·최종 출력·검증·질문) · 다른 형식 내보내기. 쉬운 모드에서도 펼쳐 사용 가능
  - **바로 표시**: 양식 · 작업 방향 · 구상·요청 · 재료 추가·관리 · 만들기 방식과 호출 안내 · 만들기 · 결과 직접 편집 · **이 칸만 다시**(누르면 칸 아래에 지시 입력·다시 쓰기·취소가 펼쳐지고 Enter로 실행. 일괄 다시 쓰기 지시와 별개) · **잘린 응답 이어서 만들기** · 고정/글 복사/글 내려받기
  - 헤더에서 켜고 끄며 상태는 저장되어 복원. 중복 안내 배너는 표시하지 않음
- **첫 연결 안내**: 선택한 연결의 모델·필수 키 등이 준비되지 않으면 만들기 버튼을 **연결 설정하고 만들기**로 표시. 누르면 연결 설정을 열며 입력한 구상은 유지. 설정 후 작업대로 돌아와 만들기를 눌러야 생성하며 자동 실행하지 않음
- **키보드 접근**: 방식·후보를 버튼으로 선택. 모달 열기 시 내부 포커스, Tab 순환, Esc 닫기와 닫은 뒤 원래 포커스 복귀

## 1. 재료 (성운)

- 드롭존 하나로 자동 판별: **PNG 카드(tEXt) · CHARX · 카드 JSON(V1~V3) · 로어북 · ST 월드인포 · TXT/MD**
- 여러 파일 한꺼번에 가능. 카드에 내장된 로어북은 **별도 항목으로 분리**
- **직접 붙여넣기**: 재료 이름 직접 입력(비우면 첫 줄→이름), **쓸 곳(세계/인물/프롬프트 복수 선택)**, 쉼표로 나누는 자유 태그, 내부 스크롤 입력창, 글자/토큰 수 실시간, Ctrl+Enter
- 새로 넣은 재료(파일·붙여넣기)는 **선택 해제 상태**로 들어온다. 결과·후보·기록·대화의 **재료로 넣기/저장**만 선택된 채로 들어오며, 이 선택은 **분류 전환·새 작업에서 전부 해제**된다(한 작업 안에서만 유효)
- 카드마다: **포함 체크박스** · 제목 옆 **세계/인물/프롬프트 용도 배지** · 토큰 추정 · SVG 아이콘 **수정/복제/삭제**. 형식 배지는 목록에서 제외하고 수정 화면의 **자료 형식**으로 표시
- 모든 불러온 재료는 즐겨찾기 여부와 상관없이 브라우저에 계속 저장되어 새로고침·재실행 뒤 복원
- 카드마다 **즐겨찾기 별표**: 목록 위 우선 표시, 일괄 삭제 보호, 폴더 분류
- **즐겨찾기 안의 재료 폴더**: 즐겨찾기를 열었을 때만 모두/미분류/폴더별 필터와 폴더 관리 도구를 표시. 폴더 줄은 스크롤바 없이 가로 드래그·터치 스와이프. 폴더 생성·이름 변경·삭제, 폴더 삭제 시 재료는 미분류로 이동
- **쓸 곳과 태그**: 형식과 별도로 세계·인물·프롬프트를 복수 지정. 인물 카드는 인물, 로어북은 세계가 기본값이며 미지정 텍스트에는 **미분류** 표시. 용도 배지는 모두 같은 회청색으로 제목 줄에 묶고 `#태그`는 아래에 표시. 쓰임새 필터 제공
- **재료 검색**: 이름·종류·출처·쓸 곳·태그(`#태그` 포함)·텍스트 본문·캐릭터 필드·로어북 항목 이름/키워드/본문 통합 검색. 카드 태그를 누르면 전체 재료에서 즉시 검색
- 카드마다 **수정**: 재료 이름·쓸 곳·태그와 텍스트 본문, 캐릭터 각 필드, 로어북 항목 이름·키워드·본문을 직접 편집. X는 닫기+이번 편집 폐기, 아래에 최초 원본과 비교 · 원본 복원 · 수정 반영
- **복제**: 현재 수정 상태를 독립된 새 재료와 새 원본으로 복제. 삭제는 휴지통 아이콘으로 통일하고 삭제 동작에 X를 쓰지 않음
- 로어북: **항목 고르기** (검색 / 전체 선택 / 해제 / **설정 항목만** — 지시문 의심 항목 자동 흐리기 + "지시문?" 표식)
- 캐릭터: **내용** (필드 미리보기)
- **즐겨찾기 외 전체 삭제** · 재료 개수 뱃지
- 재료는 세 분류가 공유. 분류별 안내문(matHint) 연동
- 양식별 재료 필요 표시: 필수인데 없으면 경고 + "재료 넣으러 가기"
- 작업대 입력 화면의 구상 아래 **사용할 재료**: 선택 개수와 **재료 추가·관리** 버튼은 바로 표시하고 목록은 기본 접기. 관리 버튼은 성운 밖에서 현재 탭을 유지한 모달을 열며, 닫을 때 편집 중 입력과 선택을 보존. 현재 분류에 맞는 재료를 `추천`으로 먼저 보여주되 자동 제외하지 않음. 두 만들기 방식에 공통 적용
- 재료가 0개여도 빈 화면의 세계/인물/프롬프트 바로가기와 상단 분류 버튼으로 작업대 진입. 새 제작은 **구상만으로 시작** 가능하며 재료 선택기는 선택 사항임을 안내

## 2. 작업대 (분류 공통 구조)

- **입력 배치**: 양식·작업 방향 → 구상·요청 → 재료 선택/추가·관리 → 만드는 방법 → 접힌 추가 설정 → 입력 상자 밖 오른쪽 만들기 버튼
- **구상·요청 통합**: 모든 단계에 전달하고 `briefBy`로 분류별 저장. 기존 `extraBy` 내용은 각 구상 뒤에 **추가 조건:**으로 병합 후 비움. 최종 출력의 `convert.extraBy`는 별도 유지
- **만드는 방법 선택**: 두 카드에 각 방식의 효과를 짧은 명사형으로 표시하고 기본 API 호출 횟수, 응답 복구 시 추가 호출 가능성을 직접 표시. 좁은 화면에서도 두 카드는 한 줄에 나란히 두고 설명은 줄바꿈. 별도 생성 도움말 ? 제거. 검증 옵션·양식·캐스트 인원에 따라 횟수 갱신
- **한 번에 만들기**: 일반 결과는 API 기본 1회, 씨앗·자동 검증 생략, 선택한 재료 원문 사용. 재료나 구상 중 하나만 있으면 실행. 입력 → 큰 로딩 화면 → 결과로 전환
- **화면 복귀**: 입력으로 돌아가도 구상·요청·재료·기존 결과를 유지. **결과 다시 보기** 또는 **진행 중인 작업 보기**는 API 없이 기존 화면을 연다. 재생성 실패·중지 시 이전 작업 보존. `project.screen`을 분류별 작업과 초안에 저장하며 이전 `card.inputOpen` 상태도 해석
- **단계별 공정**: 입력 → 정리 확인 → 후보 선택 → 결과를 한 화면씩 표시
  - 정리 후 다음 버튼으로 후보 생성. 후보 선택·섞기는 후보 화면에 머물고 **이 후보로 만들기**를 눌러 결과 생성. 상단 단계선으로 이미 만든 단계만 다시 열기
  1. **정리** — 분류별 이름·문구 (설계 준비 / 재료 정리 / 요구 정리). 응답 키를 고정하지 않고 실제 반환 항목을 모두 카드로 표시. 정리본은 **분류별 분리 보관**(digestBy), 다른 양식으로 정리한 내용이면 경고. 지우기
  2. **후보 선택(별씨앗)** — 후보 N개, 하나/여럿 선택(고른 것은 모두 금색), **고른 후보 섞기**. 하나를 골라야 다음 생성 가능. 후보 카드마다 **잠그기**(다시 뽑아도 남고 모델에 '겹치지 않게' 전달) · **재료로 저장**(한 줄+출발점+역할을 현재 분류 재료로, 한 번만) · **이걸로 대화**(재료 저장 후 대화 탭으로 이동, 재료 컨텍스트 켬, 상대 역할을 분류에 맞춤). `skipSeed` 양식도 정리 확인에서 멈추고 **결과 만들기**를 누르면 다음 단계 실행
  3. **결과** — 제목·미리보기의 접이식 카드로 표시. 첫 카드만 기본으로 열고 개별 또는 전체 펼치기/접기, 칸 직접 편집·다시 만들기. 잠금·일괄 다시 쓰기·원문·최종 출력·검증·질문은 접힌 **추가 도구**에서 접근. 도구를 펼치면 칸별 잠금도 표시
     - **이어서 만들기**: 세계·인물·프롬프트 공통. 선택 지시를 비우면 잘린 칸/마지막 칸부터, 지시를 적으면 그 방향으로 새 내용만 기존 필드 뒤에 병합. Ctrl+Enter 지원
     - 확장·한 번에 만들기의 JSON이 중간에 끊기면 API로 한 번 자동 이어받기. 그래도 끝나지 않으면 스키마 키별로 읽힌 문자열을 부분 복구하고 `truncated` 상태로 이어쓰기 UI를 강조
     - **최종 출력 만들기**: 결과를 덮어쓰지 않고 별도 최종본 생성. 원본 언어/번역 언어 자유 입력, 번역 · 핵심 유지/토큰 최적화 · YAML · 약 N자로 요약 · 원본 언어 뜻풀이를 조합하고 마지막 추가 요청까지 기본 API 1회로 반영(잘림/형식 오류 때 자동 후속 호출 가능). 마지막 추가 요청은 분류별 저장, 새 작업에서 초기화. 자동 복구 뒤에도 잘리면 읽힌 문자열까지 먼저 표시하고 **잘린 부분 이어서**, 평소에는 **이어서**로 최종본·뜻풀이의 새 부분만 병합. 뜻풀이 보기/숨기기, 전후 추정 토큰·감소율·목표/실제 글자 수·이어 쓴 횟수와 원본 수정 후 오래된 최종본 경고 표시
  - **검증** — 결과의 추가 도구 안에 위반 인용과 판정 표시. 접힌 요약에도 판정을 짧게 표시. 검증 도중 중지해도 완성 결과는 보존. "이 문제 어떻게 할까?" → 질문 연계. `skipCheck` 양식은 검증 패널 숨김
- **구상 칸**: 라벨은 세 분류 모두 "구상". 기본 2줄·80px, 수동 세로 확대 가능. ‘선택’·반복 안내문은 제거하고 분류별 예시만 제공. 프롬프트 예시는 문체·감정선 중심. 양식의 긴 설명은 첫 작업 화면에서 제외. 재료 제목·선택 개수·관리 버튼은 한 줄에 놓고 제목만 굵게 표시하며 목록과 하나의 상자로 묶음
- **분류별 작업 복원**: `workBy`에 양식·정리·후보/선택·결과·잠금·문답·검증·캐스트/관계·최종본과 이어쓰기/다시 쓰기 지시를 보관하고, 해당 양식이 사용 가능하면 분류로 돌아올 때 복원. 분류를 떠나기 전 대기 중인 결과 편집 저장을 먼저 반영
- **새 작업**: 현재 분류의 입력·중간 결과·잠금·문답·캐스트·최종본을 확인 후 초기화. 다른 분류의 작업과 재료·기록은 유지
- **작업 방향**: 세계(새로 만들기/기존 세계 다듬기), 인물(세계에서 뽑기/기존 인물 다듬기/상대역/관계망), 프롬프트(새로 만들기/기존 프롬프트 수정). 카드 제목은 위쪽 정렬. 프롬프트 수정의 같은 용도 다듬기(`supplement`)/다른 용도 바꾸기(`adapt`)는 추가 설정에서 선택하며 기존 값 보존
- **다듬는 방향**: 추가 설정에서 균형 있게 / 단순하게 / 자세하게 / 다시 설계 선택. 세계·인물·프롬프트별 선택을 따로 저장하고 모든 관련 API 단계에 적용
- 재료 본문이나 포함 선택이 바뀌면 정리본에 **다시 정리 권장** 경고
- **추가 설정**: 기본 접기. 출력 언어 · 전체 톤 · 후보 수 · 성인 요소 · 검증 여부. 캐스트 인원은 인물 분류에서 표시
- **출력 칸 끄기**(fieldToggleBox, 추가 설정 안): 현재 양식의 칸을 알약 토글로 나열하고 끈 칸은 생성·표시·다시 쓰기·잠금에서 제외(`activeSchema()`). 양식별로 `S.opts.fieldOff[presetId]`에 저장. 최소 1칸은 남김. OOC 지시문 양식과 칸이 1개인 양식에서는 숨김. 양식 편집기는 전체 schema를 그대로 씀
- **결함·비밀의 선**(`sensitivityDirection`): 인물·세계 분류의 후보·섞기·결과·다시 쓰기 공정에 고정 규칙으로 붙는다. 배신·빚·죽음의 책임 같은 서사적 비밀은 허용, 성병·성폭력·근친·자해·유산·아동 가해는 구상·재료에 명시된 경우에만. 비극을 한 인물에 겹쳐 쌓지 않게. 옵션 UI 없음 — 구상에 적으면 덮어씀
- **캐스트**: 생성한 인물 목록을 별도 화면에 표시. 개별 결과 열기·캐스트로 돌아가기, 관계 짜기·기록에 넣기 제공. 중지 시 그때까지 완성한 인물 보존. 기본 호출은 정리 1회 + 인원당 후보/결과 각 1회, 관계 생성은 별도
- **결과에 질문하기**(btnAsk): 설계 상담역 / 당사자 본인 2모드, 제안 문안 원클릭 반영, 문답 비우기
- 작업대 API 호출 중 입력·결과를 감추고 큰 로딩 화면에 현재 단계·실제 호출 차수·**요청 중지** 표시. 실패·중지는 이전 화면으로 복귀하며, 완성 결과와 캐스트 부분 결과는 보존. 다른 화면의 호출은 실행 단추 옆 중지 버튼 유지
- 요청 중 분류·양식·작업 입력 교체와 다른 요청 실행을 제한하고 응답 반영 전 작업 유효성을 확인
- **내보내기**: 글로 복사 / 글로 내려받기 / 인물 결과의 카드 JSON 복사·내려받기와 **PNG 카드** / **로어북으로 뽑기**(btnDlBook, 세계→ST 월드인포+재료 자동 추가) / **재료로 넣기**(분류 연쇄 제작) / **고정하기**(btnSaveLib — ★ 붙여 기록에 저장)
- **이 양식 등록하기**(btnMakePreset): 프로필 양식 설계(JSON) 결과를 즉시 양식으로 등록
- **OOC 지시문**(양식 id `ooc`, 프롬프트 분류): 양식을 고르면 시작 방식 아래에 **OOC 형식** 패널(oocPanel)이 나타난다. 형식 6종(장면형·선택·비교형·양식·기록형·창작 결과물형·분기형·시각 연출형)을 체크로 복수 선택하고 **전체 선택/전체 해제** 제공. 고른 형식이 곧 결과 칸(`syncOocSchema`가 저장된 양식의 schema를 교체)이라 칸별 잠금·**이 칸만 다시**·이어쓰기가 형식별 수정이 된다. 안 고르면 `ooc`·`note` 두 칸으로 모델이 형식 하나를 자동 선택. **호칭 표기**(PC/NPC · {{user}}/{{char}} · 재료 이름)와 **역할 반전 버전 함께**(형식마다 `_rev` 칸 추가) 옵션은 `S.opts.ooc`에 저장·백업. 설정은 `oocDirection()`으로 모든 공정과 한 번에 만들기에 사용자 메시지로 주입되며 다른 양식에는 붙지 않는다. 구상 placeholder와 다시 쓰기 placeholder는 OOC 전용 예시로 바뀐다

## 3. 대화 (교신)

- **반응형 배치**: 981px 이상은 왼쪽 설정·오른쪽 채팅, 980px 이하는 설정을 기본 접기. 뷰포트 높이에 맞춰 입력창을 유지하며 설정·대화 기록만 각각 스크롤
- **대화 도구·입력**: 토큰 정보 오른쪽에 **작업대 반영·대화 요약**, 700px 이하에서는 다음 줄 오른쪽. 입력칸·보내기 기본 48px, 입력칸은 내용에 따라 최대 160px(700px 이하 120px)까지 자동 확장 후 내부 스크롤. 보내기는 같은 테두리 안 오른쪽 아래 정렬. 초안 복원·화면 폭 변경에도 높이 갱신
- 상대 역할 5종: 세계관/인물/프롬프트 상담역 · 냉정한 평가자 · 역할 없음
- **역할 프롬프트 편집**: 직접 입력(customTalkPrompts 저장) + 기본 프롬프트 보기 + 기본값으로 되돌리기
- **보낼 내용**: 작업 재료 / 정리한 내용 / 지금 만든 것을 독립 선택. 작업 재료는 성운 항목별 선택, 종류별·전체 토큰 분량 표시
- **눈 버튼**: 완료한 문답(질문+이어진 답변)을 다음 전송에서 제외/포함. 원문과 제외 상태는 저장. 요약에도 별도 눈 버튼 제공. 최근 24개 포함 메시지를 보내되 첫 답변이 질문 없이 남으면 제외
- **제외 범위**: 다음 대화·대화 요약·작업대 반영·재료 내보내기에 같은 필터 적용. 요약할 때 제외한 문답/요약은 화면에 보존. 대화 전체 복사는 제외한 원문도 포함
- 보내기(Ctrl+Enter) · 패널 머리 iconbtn: 대화 비우기 · **보내는 대화 재료로** · **대화 전체 복사**
- 분류 바꾸면 상대 역할 연동 — 단 **냉정한 평가자(critic)와 역할 없음(free)은 유지**
- **실패 메시지 보존**: 전송 실패·중지 시 원문과 오류를 남기고 **다시 보내기 / 삭제** 제공. 실패 메시지를 처리하기 전에는 새 메시지 전송을 막고 작성 중인 입력 유지
- 대화와 작성 중인 입력 저장(새로고침 유지). 응답 중 중복 전송 차단

## 4. 기록 (성도)

- 만들면 **자동 저장**(1.2초 디바운스), 이어 고치면 새 항목 없이 **갱신**(libId). 원본 결과와 최종본의 이어쓰기 병합 결과·횟수와 잘림 상태, 최종 출력 옵션·추가 요청·원본 언어 뜻풀이도 기록에 보존
- ★ 고정 · 이름 변경 · 검색 · 분류 필터(분류 전환 시 자동 연동) · **불러오기**(양식 자동 전환)
- 패널 머리 iconbtn: 전체 내보내기 · 비우기(고정 안 한 것만; 최대 200개, 초과 자동 정리 시 로그와 화면 알림)
- 기록에서 바로 글/JSON/PNG 내보내기
- **미리보기 모달**: 기록 항목 이름을 누르면 읽기 전용으로 내용을 바로 봄(칸별 제목+본문). 모달 안에 이름 바꾸기·작업대로 불러오기·**재료로 넣기**·글/카드JSON/PNG 액션을 모음. 목록 줄의 이름 클릭은 이름 바꾸기가 아니라 미리보기 열기(이름 바꾸기는 모달로 이동). Esc·바깥 클릭으로 닫힘
- **재료로 넣기**: 기록 항목을 성운 재료(text)로 편입해 다른 양식에서 이어 씀. 별표(고정)와는 별개 기능
- **고정하기 / 캐스트 기록에 넣기** 후에도 임시 제작 상태를 유지해 다음 실행에서 중간 작업 복구 가능

## 5. 양식 (프리셋) — 15종 내장

- 점검 3종(세계·인물·프롬프트) 공통 칸: 한 줄 진단 · **살아 있는 곳** · 흔한 곳 · 채우면 좋을 것 · 정해야 할 질문
- 프롬프트: 프롬프트 생성기 · 롤플레이 프롬프트 · **OOC 지시문** · 프롬프트 점검
- 세계: 세계관 설계 · 압축 소개본 · 상세 안내서 · 세계 점검
- 인물: 기본 카드 · 창작 엔진 (풀 시트) · 설정집 프로필 (압축형) · 첫 만남 (그리팅) · 성향 프로토콜 (성인 · 18+) · 인물 점검
- 공통(group:'all'): 프로필 양식 설계 (JSON)

> **비활성 정의**: `presetAudit()` / `평가 · 보강 제안`(id `audit`, group `all`)은 코드에 정의되어 있으나 `builtinPresets()`에 없어 목록에 뜨지 않는다. 세 종류 점검 양식의 뼈대(`auditBase()`)로만 쓰이므로 **지우지 말 것**.
> 은퇴는 확정이다 — `group:'all'`이라 되살리면 세 분류마다 점검 양식이 2개가 되어 고를 근거가 없어진다. 대신 이 양식에만 있던 고유 칸 `working (살아 있는 곳)`은 2026-08에 점검 3종 전부로 이식했다.

- **공통 지시문**: 구획 목록(켜기끄기 토글·이름·역할칩·토큰·위/아래/삭제·접이식 편집). 모든 공정 + 한 번에 만들기 맨 앞에 주입
- **출력 칸**: 접이식(키·이름·지시), 순서 바꾸기, 추가/삭제 — 칸이 곧 잠금·재굴림 단위
- **공정별 지시문**: 접이식(블록 수·토큰·temperature 요약), temperature/max tokens
- 치환 가능한 값: `{{source}} {{digest}} {{seed}} {{card}} {{schemaSpec}} {{lang}} {{tone}} {{seedCount}} {{extra}} {{cast}} {{locked}} {{unlocked}} {{instruction}}`
- 프리셋 도구(아이콘 6개): 복제 · 새로 만들기 · 이름 바꾸기 · 내보내기 · **가져오기(ST 프리셋 포함 — Orrery·ST 자동 판별)** · 삭제 / 그리고 공정별 지시문 아래 기본값으로 되돌리기(btnResetStages — **그 양식의 내장 기본값**으로 되돌림. 직접 만든 양식이면 거부 토스트)
- ST 프리셋 가져오기: 내장 식별자·마커(ST_BUILTIN) 제외하고 **공통 지시문으로** 들어옴

## 6. 연결 — 프로바이더 14종

openai · anthropic · gemini · vertex · openrouter · nanogpt · mistral · cohere · xai · together · venice · pollinations · local(Ollama · LM Studio · koboldcpp) · custom

- 연결 추가/편집/삭제 · 헤더 알약에서 전환 · **확인**(실제 API 1회, 짧은 입력, 응답 최대 24토큰, 연결별 응답 상한으로 덮어쓰지 않음, connDot 초록/빨강, 업체가 usage를 주면 실제 입력/출력 토큰 표시). 마지막 확인 시각·토큰을 연결 카드에 지속 표시하고 연결 설정 변경 시 무효화 · 모델 목록 받기
- 연결 입력은 업체·키·모델을 먼저 표시하고 주소·응답 길이 등은 **세부 연결 설정**으로 접음. 로컬·사용자 지정 연결은 세부 설정 기본 펼침
- 연결별 파라미터: 응답 토큰 상한 · **컨텍스트 상한(전송 전 차단 + 안내)** · temperature · top_p(Cohere는 p)
- vertex는 project/location/액세스 토큰, pollinations는 키 없음
- **API 연결 백업 / API 연결 불러오기**: 키·액세스 토큰과 업체·모델·주소·연결 옵션만 JSON으로 이동. 키가 평문으로 포함됨을 안내. 현재 목록에 병합하며 같은 연결 id만 갱신하고 키가 생략되면 기존 키 보존. 다른 연결과 현재 작업·재료·대화는 유지하며 가져오기 자체는 API를 호출하지 않음
- **전체 백업/백업 불러오기**(btnDataExport/Import): 연결·양식·옵션·성도 기록 전체는 항상 포함. 재료(즐겨찾기·폴더 구조 포함)·현재 작업/중간 결과·대화는 기본 포함, API 키는 기본 제외. 키 없는 백업을 가져오면 같은 연결 id의 현재 키를 보존. 실행 로그는 항상 제외. `backupVersion:2`, 구형 설정 백업도 가져오기 호환
- 도움말(?): CORS 안내 모달 + `python -m http.server 8777` 복사 (Esc로 닫힘)

## 7. 로그

- 실행 로그 · 전부 복사 · 비우기 · "보낸 지시문 전문까지" 옵션 · 응답 usage가 있으면 실제 입력/출력 토큰 기록(호출 전 `토큰쯤`은 글자 수 기반 추정)

## 8. 저장·이관

- localStorage `orrery.v1`: 연결·양식·옵션·기록과 함께 모든 재료(`assets`)·즐겨찾기 상태·재료 폴더(`assetFolders`) 저장. 쓰지 않는 카드 raw는 제거하고 수정 전 원본은 첫 편집 때만 생성 · 이전 `favoriteAssets`와 구버전 키(`vivarium.v1` / `terrarium.v1` / `casting.v1`) 불러오기 호환
- localStorage `orrery.draft.v1`: 현재 제작 공정·옵션과 `workBy`의 다른 분류 작업을 임시 보관. 이어쓰기·잘림 복구·최종본과 현재 화면(`project.screen`) 포함. 재료는 `orrery.v1`에 항상 저장. 다음 실행 때 공정 복구 여부 확인, 고정/캐스트 기록 후에도 유지
- 저장 성공은 잠깐 상태 표시, 저장 실패는 화면에 계속 경고하고 전체 백업을 안내. 손상된 저장 원문은 `orrery.recovery.v1`에 보존

---

## 동반 파일

- `README.md` — 사용 설명서
- [tests/README.md](tests/README.md) — API 모의 응답을 쓰는 동작 회귀 검사와 Chrome/Edge 배치 검사 실행법·검증 범위

## 알려진 검증 한계 (코드로 못 잡는 것)

Chrome의 지정 화면 크기에서 작업대·대화 배치는 실제 렌더링으로 검사함. 같은 검사 스크립트는 Edge에서도 실행 가능. 확인 안 됨: 실물 기기의 터치·가상 키보드 조작 · 실제 업체 API 연결·14개 양식의 생성 품질 · 프로바이더별 CORS 실측 · Safari 저장 · SillyTavern 호환. 모의 응답과 배치 검사로 대신 검증하지 않음.
